export type ChatSettings = {
  wordLength: number;
  wordNum: number;
};

export type ScoreDict = {
  correctNum: number;
  historyNum: number;
};

export type GameDict = {
  isGame: boolean;
  todayScore: number;
};
